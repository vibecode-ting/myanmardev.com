<section id="how" style="padding:clamp(3rem,7vw,5rem) clamp(1.25rem,4vw,3rem);border-top:1px solid #1D232B;border-bottom:1px solid #1D232B;background:color-mix(in srgb,var(--wash) 60%,transparent)">
  <div style="max-width:78rem;margin:0 auto">
    <div class="sx rise"><b></b> <span>/ pipeline</span></div>
    <h2 class="h-section rise" style="margin-top:.9rem;max-width:30rem">Order. Pay. Deploy. Zero human touch.</h2>
    <ol id="tg-ledger" style="margin-top:2.5rem;list-style:none">
      <li class="rise" style="display:grid;grid-template-columns:auto 1fr auto;align-items:baseline;gap:1.5rem;padding:1.5rem 0;border-top:1px solid #1D232B">
        <span class="mono" style="color:var(--accent);font-size:1.5rem;font-weight:700">No.01</span>
        <div>
          <h3 style="font-family:var(--display);font-weight:500;font-size:clamp(1.25rem,2.4vw,1.65rem)">Pick your subdomain</h3>
          <p style="font-size:.9375rem;color:color-mix(in srgb,var(--ink) 60%,transparent);margin-top:.3rem;max-width:40rem">Choose any available name under <span class="mono" style="color:var(--accent);font-size:.8125rem">.myanmardev.com</span>. Our system checks DNS availability instantly against live Cloudflare records.</p>
        </div>
        <span class="mono" style="color:color-mix(in srgb,var(--ink) 60%,transparent);font-size:.75rem;white-space:nowrap;text-align:right">availability check<br>~200ms</span>
      </li>
      <li class="rise" style="display:grid;grid-template-columns:auto 1fr auto;align-items:baseline;gap:1.5rem;padding:1.5rem 0;border-top:1px solid #1D232B">
        <span class="mono" style="color:var(--accent);font-size:1.5rem;font-weight:700">No.02</span>
        <div>
          <h3 style="font-family:var(--display);font-weight:500;font-size:clamp(1.25rem,2.4vw,1.65rem)">Choose platform & confirm</h3>
          <p style="font-size:.9375rem;color:color-mix(in srgb,var(--ink) 60%,transparent);margin-top:.3rem;max-width:40rem">Connect to GitHub Pages, Vercel, Netlify, or any custom CNAME. We map your subdomain to your live project endpoint automatically.</p>
        </div>
        <span class="mono" style="color:color-mix(in srgb,var(--ink) 60%,transparent);font-size:.75rem;white-space:nowrap;text-align:right">4 platforms<br>1 click each</span>
      </li>
      <li class="rise" style="display:grid;grid-template-columns:auto 1fr auto;align-items:baseline;gap:1.5rem;padding:1.5rem 0;border-top:1px solid #1D232B;border-bottom:1px solid #1D232B">
        <span class="mono" style="color:var(--accent);font-size:1.5rem;font-weight:700">No.03</span>
        <div>
          <h3 style="font-family:var(--display);font-weight:500;font-size:clamp(1.25rem,2.4vw,1.65rem)">DNS created. You go live.</h3>
          <p style="font-size:.9375rem;color:color-mix(in srgb,var(--ink) 60%,transparent);margin-top:.3rem;max-width:40rem">The worker creates the CNAME record via Cloudflare API the moment your payment succeeds. No approval queue. No support ticket. Your subdomain routes traffic instantly.</p>
        </div>
        <span class="mono" style="color:var(--accent);font-size:.75rem;white-space:nowrap;text-align:right">instant deploy<br>0 waiting</span>
      </li>
    </ol>
  </div>
</section>

<style>
  @media (max-width: 640px) {
    #tg-ledger li {
      grid-template-columns: minmax(0, 1fr) !important;
      gap: 0.5rem !important;
    }
    #tg-ledger li span:last-child {
      text-align: left !important;
    }
  }
</style>
